import styled from "styled-components";

export const Styled = {
    Wrapper: styled.div`
        min-height: 100vh;
        background: var(--color-bg);
        padding: 24px 16px 40px;
    `,

    Inner: styled.div`
        max-width: 1200px;
        margin: 0 auto;
    `,
};
